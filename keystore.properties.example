# 复制为 keystore.properties 后填入你自己的值（该文件不会入库）。
#
# 生成密钥：
#   keytool -genkeypair -v -keystore demo-release.jks -alias liquidglass \
#     -keyalg RSA -keysize 2048 -validity 10000
#
# 不配置也能构建，只是 release 产物为未签名 APK，无法直接安装。
storeFile=demo-release.jks
storePassword=
keyAlias=liquidglass
keyPassword=
